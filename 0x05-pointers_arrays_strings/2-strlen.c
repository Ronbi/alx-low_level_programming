#include "main.h"
/**
 * main -  function that returns the length of a string
 *
 * Return: void that means our answer is correct
 */
int _strlen(char *s)
{
	int d;

	d = 0;
	while (s[d] != '\0')
	{
		d++;
	}

	Return (d);
}
